name: crisis_app
description: Crisis Response Mobile Application
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  google_generative_ai: ^0.4.3
  provider: ^6.1.1
  http: ^1.1.2
  geolocator: ^10.1.0
  shared_preferences: ^2.2.2
  firebase_core: ^3.0.0
  firebase_messaging: ^15.0.0
  firebase_database: ^11.0.0
  cloud_firestore: ^5.0.0
  firebase_storage: ^12.0.0
  url_launcher: ^6.2.5
  image_picker: ^1.0.7
  camera: ^0.10.5+9
  jwt_decoder: ^2.0.1
  record: ^5.2.0
  audioplayers: ^5.2.1
  path_provider: ^2.1.2
  path: ^1.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
